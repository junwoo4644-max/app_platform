# SQLite file lives in the mounted /app/data volume in Docker.
DATABASE_URL="file:./data/portal.db"
DATA_DIR="./data"

# openssl rand -base64 32
NEXTAUTH_SECRET="랜덤 문자열로 교체하세요"
NEXTAUTH_URL="http://localhost:3000"

# 최초 실행 시 이 계정으로 로그인할 수 있는 관리자 계정이 자동 생성됩니다.
ADMIN_EMAIL="you@example.com"
ADMIN_PASSWORD="바꿔주세요"
